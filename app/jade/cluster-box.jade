.box.cluster-box
  .main-content
    .animation
      .svg-holder
      .title
    .white-box
      .corner-box
        img.shadow-icon(data-src="corner-bg" )
      if scalesHoriz
        .total=totalMembers
      .id
        .name=name
        .hosts <span>Cluster : </span>#{clusterName}
      .stats-strip
  .service-bug  
    .bg-hex
    img.shadow-icon(data-src="hex-#{serviceType}" )
  .nav-holder
  .sub
    .sub-content
    
